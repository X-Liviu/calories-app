# ===============================
# MongoDB connection strings
# ===============================

# Production database
MONGODB_URI=mongodb+srv://<db-username>:<db-password>
@clustername.(specialcode).mongodb.net/(nameApp)?retryWrites=true&w=majority&appName=(clustername)

# Test / development database
MONGODB_URI_TEST=mongodb+srv://<db-username>:<db-password>
@(clustername).(specialcode).mongodb.net/(nameAppTest)?retryWrites=true&w=majority&appName=(clustername)

# ===============================
# Application settings
# ===============================

# Port where your backend server will run
PORT=<port>

# Secret key for JWTs, sessions, or other cryptographic operations
SECRET=<secret>

# ===============================
# Notes
# ===============================
# 1. Replace all fields in angle brackets (<...>) with your own credentials.
# 2. All the fields in parenthesis will be unique for each user on your MongoDB Atlas cluster.
# 3. Do NOT commit your real credentials to version control. Use .gitignore if needed.
# 4. You can copy this template to a file named ".env" and fill in your values.